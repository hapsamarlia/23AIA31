# Logging Middleware

This project is built using React and JavaScript.
It demonstrates a simple component-based application with a Header, Button, and Response.
The code is organized into reusable components for better readability and maintenance.
